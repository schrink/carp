source:
  type: oura
  socket: "/app/node-ipc/node.sock" # Putanja unutar kontejnera (bind mount)
  bearer: Unix

sink:
  type: cardano
  db:
    type: postgres
    database_url: postgresql://carp:1234@postgres:5432/carp_vector_mainnet
  network: "764824073"

start_block:

